class Job {
  final String name;
  bool isSelected;

  Job({required this.name, this.isSelected = false});
}

class LagosLocations {
  final String name;
  bool isSelected;

  LagosLocations({required this.name, this.isSelected = false});
}
